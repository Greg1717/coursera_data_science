makeVector <- function(x = numeric()) {
        m <- NULL
        set <- function(y) {
                x <<- y
                m <<- NULL
        }
        get <- function() x
        setmean <- function(mean) m <<- mean
        getmean <- function() m
        list(set = set, get = get,
             setmean = setmean,
             getmean = getmean)
}

cachemean <- function(x, ...) {
        m <- x$getmean()
        if(!is.null(m)) {
                message("getting cached data")
                return(m)
        }
        data <- x$get()
        m <- mean(data, ...)
        x$setmean(m)
        m
}

aVector$set(30:90000)          # reset value with a new vector
debugonce(cachemean)
cachemean(aVector)          # notice mean calculated is mean of 30:50, not 1:10

environment(aVector$set)
environment(aVector$get)
environment(aVector$getmean)
